# Narrow Recon Methodology — Single-Host Playbook

> **AUTHORIZED USE ONLY** — Perform reconnaissance only against assets explicitly within the approved scope.

## Repository-ready version

This playbook documents a narrow, single-host reconnaissance workflow from manual application mapping through final recon summary.

A structured reconnaissance workflow for authorized security assessments
AUTHORIZED USE ONLY
GitHub publishing note: This document is structured as a human-readable playbook. For repository use, pair it with a Markdown README and keep raw scan outputs in a dedicated evidence directory.
فهرست مطالب
پیش از شروع
چک‌لیست آمادگی عملیاتی (Pre-Engagement Checklist)
پیش از اجرای هر مرحله از این سند موارد زیر باید تأیید شده باشند
- [ ] مجوز کتبی برای انجام تست دریافت شده است
- [ ] مشخص و مستند شده است (Scope) محدوده دقیق تست
- [ ] مشخص شده‌اند (Out of Scope) بخش‌های خارج از محدوده
- [ ] های مجاز برای تست مشخص شده‌اند IP دامنه‌ها، زیر‌دامنه‌ها یا
- [ ] مشخص شده است (Black Box / Gray Box / White Box) نوع تست
- [ ] حساب‌های کاربری و سطح دسترسی موردنیاز (در صورت وجود) دریافت شده است
- [ ] بازه زمانی مجاز برای اجرای تست تعیین شده است
- [ ] قالب ثبت یافته‌ها و محل ذخیره‌سازی مستندات مشخص شده است

## مرحله 1 — شناخت دستی اپلیکیشن
Manual Recon
چک‌لیست بررسی دستی
ساختار کلی اپلیکیشن
- [ ] صفحه اصلی و عملکرد کلی سایت
- [ ] صفحات اصلی و منوهای ناوبری
- [ ] ساختار و الگوی URLها
- [ ] صفحات خطا403، 404، 500
- [ ] رفتار Redirectها
احراز هویت
- [ ] وجود یا عدم وجود Login
- [ ] وجود یا عدم وجود Register
- [ ] فرآیند Logout
- [ ] فرآیند Forgot Password
- [ ] فرآیند Change Password
- [ ] وجود یا عدم وجود MFA
حساب کاربری
- [ ] پروفایل کاربری
- [ ] صفحه تنظیمات
- [ ] انواع نقش‌های کاربری Guest، User، Admin و...
- [ ] تفاوت قابلیت‌های هر نقش (در صورت امکان)
قابلیت‌های اپلیکیشن
- [ ] فرم‌های موجود
- [ ] جستجو (Search)
- [ ] بارگذاری فایل (Upload)
- [ ] دانلود فایل (Download)
Import☐
Export☐
- [ ] اعلان‌ها (Notifications)
- [ ] صفحات تماس یا ارسال بازخورد
نقاط ورود اطلاعات (Input Points)
- [ ] تمام فیلدهایی که کاربر می‌تواند داده وارد کند شناسایی شوند.
مانند:
Login
Search
Contact Form
Comment
Profile
Upload
Address
Feedback
فایل‌ها و صفحات عمومی
- [ ] وجود robots.txt
- [ ] وجود sitemap.xml
- [ ] وجود security.txt
- [ ] وجود favicon.ico
فقط وجود یا عدم وجود آن‌ها ثبت شود؛ تحلیل تخصصی این فایل‌ها در مراحل بعد انجام می‌شود.
سایر مشاهدات
- [ ] زبان‌های قابل انتخاب
- [ ] رفتار Session بعد از Logout
- [ ] پیام‌های موفقیت و خطا
- [ ] فایل‌های قابل دانلود
- [ ] لینک‌های خارجی و سرویس‌های شخص ثالث (در صورت مشاهده)
بهترین شیوهٔ اجرا
ابتدا اپلیکیشن بدون ورود به حساب کاربری بررسی شود. سپس در صورت وجود حساب کاربری، با یک کاربر عادی وارد سیستم شوید و تفاوت رفتار، صفحات و قابلیت‌ها قبل و بعد از احراز هویت را مستند کنید. در صورت دسترسی به نقش‌های مختلف، تفاوت قابلیت‌ها و مسیرهای قابل دسترس هر نقش نیز بررسی شود.

### خروجی مورد انتظار
نقشه اولیه صفحات و ساختار اپلیکیشن
فهرست قابلیت‌های موجود
فهرست نقاط ورود اطلاعات (Input Points)
نقش‌های کاربری شناسایی‌شده
صفحات و قابلیت‌های نیازمند بررسی بیشتر
سطح حمله اولیه (Initial Attack Surface)

## مرحله 2 — تحلیل پاسخ‌های سرور
HTTP Analysis
دستور اجرا 1
توضیح دستور
s : -s اجرای درخواست در حالت Silent و حذف نمایش پیام‌های اضافی مانند Progress Meter.
: -D - نمایش Headerهای پاسخ HTTP در خروجی ترمینال - به معنی ارسال Headerها به خروجی استاندارد است
:  -o /dev/null نادیده گرفتن و عدم ذخیره Body پاسخ؛ در نتیجه فقط Headerهای HTTP مشاهده می‌شوند
دستور اجرا 2
توضیح دستور
ارسال هدر Origin سفارشی برای بررسی رفتار CORS
I :  دریافت فقط هدرهای پاسخ
بهترین شیوهٔ اجرا
ابتدا روی صفحهٔ اصلی و سپس روی صفحات حساس مانند ورود و پنل مدیریت اجرا شود تا اختلاف رفتار هدرها مشخص شود
- [ ] Headerهای پاسخ HTTP بررسی شدند.
- [ ] Server Header بررسی شد.
- [ ] Cookieهای ایجادشده بررسی شدند.
- [ ] ویژگی‌های Secure، HttpOnly و SameSite بررسی شدند.
- [ ] Security Headerها بررسی شدند.
- [ ] رفتار CORS بررسی شد.
- [ ] تفاوت پاسخ صفحات عمومی و حساس بررسی شد.
- [ ] متدهای HTTP قابل مشاهده ثبت شدند.
خروجی مورد انتظار:
فهرست HTTP Headerهای پاسخ
اطلاعات Cookie و ویژگی‌های امنیتی آن
وضعیت Security Headerها
رفتار CORS
متدهای HTTP قابل مشاهده
تفاوت رفتار صفحات عمومی و حساس

## مرحله 3 — بررسی گواهی و رمزنگاری ارتباط
SSL/TLS Analysis
دستور اجرا ۱ — بررسی نسخه‌های TLS و Cipher Suiteها
توضیح دستور
--script ssl-enum-ciphers  :نمایش نسخه‌های TLS و Cipher Suiteهای قابل استفاده
-p 443  :بررسی سرویس HTTPS روی پورت ۴۴۳
دستور اجرا ۲ — بررسی اطلاعات گواهی
توضیح دستور
--script ssl-cert   :استخراج اطلاعات گواهی دیجیتال
-p 443  :بررسی سرویس HTTPS روی پورت ۴۴۳
بهترین شیوهٔ اجرا
بررسی روی دامنه اصلی و در صورت وجود سرویس‌های حساس مانند API، پنل مدیریت و ساب‌دامین‌های مهم نیز تکرار شود. در صورت مشاهده تفاوت در نسخه‌های TLS یا گواهی، هر سرویس به‌صورت جداگانه مستندسازی شود.
- [ ] نسخه‌های TLS فعال ثبت شدند.
- [ ] نسخه‌های قدیمی TLS بررسی شدند.
Cipher Suite☐ های فعال بررسی شدند.
- [ ] اطلاعات Certificate استخراج شد.
- [ ] تاریخ انقضا Certificate بررسی شد.
- [ ] صادرکننده (Issuer) و دامنه‌های موجود در Certificate بررسی شدند.
- [ ] تفاوت تنظیمات SSL/TLS بین سرویس‌های مختلف بررسی شد.

### خروجی مورد انتظار
نسخه‌های TLS فعال
Cipher Suiteهای قابل استفاده
اطلاعات Certificate(Subject، Issuer و تاریخ اعتبار)
دامنه‌های موجود در Certificate (SAN)
وضعیت کلی پیکربندی SSL/TLS
سرویس‌های دارای تنظیمات متفاوت یا نیازمند بررسی بیشتر

## مرحله 4 — شناسایی فناوری
Technology Fingerprinting
دستور اجرا
توضیح دستور
v:نمایش خروجی کامل شامل تمام فناوری‌های شناسایی‌شده و جزئیات بیشتر
بهترین شیوهٔ اجرا
ابتدا روی صفحه اصلی اجرا شود.
سپس روی صفحات مهم مانند Login، Dashboard یا پنل مدیریت نیز اجرا شود.
- [ ] وب‌سرور (Apache / Nginx / IIS ...)
- [ ] سیستم مدیریت محتوا (CMS)
- [ ] فریمورک Backend
- [ ] زبان برنامه‌نویسی
- [ ] فریمورک یا کتابخانه Frontend
- [ ] نسخه فناوری‌ها (در صورت نمایش)
CDN☐
WAF ☐در صورت قابل تشخیص بودن
- [ ] اطلاعات افشاشده در Headerها یا Bannerها

### خروجی مورد انتظار
Web Server
CMS
Programming Language
Backend Framework
Frontend Framework / Library
Technology Versions در صورت وجود
CDN
WAF در صورت شناسایی
سایر فناوری‌های شناسایی‌شده
۱ WAF.
گاهی WhatWeb می‌تواند وجود WAFرا تشخیص دهد. این اطلاعات برای مراحل بعدی بسیار مهم است، چون روی نوع تست و نرخ درخواست‌ها تأثیر می‌گذارد.
۲. نسخه فناوری‌ها
اگر 2 آسیب‌پذیری‌های شناخته‌شده تطبیق داد. حتی اگر همیشه نسخه را پیدا نکنی، بهتر است در چک‌لیست و خروجی مورد انتظار جایی برای ثبت آن داشته باشی.

## مرحله 5  —خزش سایت
Web Crawling
دستور اجرا
توضیح دستور
-u:تعیین URL هدف
:-d 3تعیین عمق خزش تا سه سطح
-jc:استخراج URLها و Endpointهای موجود در فایل‌های JavaScript
-o:ذخیره خروجی در فایل مشخص‌شده
بهترین شیوهٔ اجرا
  ابتدا خزش روی دامنه اصلی انجام شود.
  در صورت وجود صفحات احراز هویت، با دسترسی مجاز و در محدوده تست، بخش‌های واردشده نیز بررسی شوند.
  خروجی قبل از استفاده در مراحل بعدی:
Duplicateها حذف شوند.
URLهای غیرمرتبط جدا شوند.
Endpointهای حساس دسته‌بندی شوند.
URL☐ های داخلی استخراج شدند.
- [ ] فایل‌های JavaScript شناسایی شدند.
Endpoint☐ های موجود در منابع سمت کاربر ثبت شدند.
- [ ] مسیرهای دارای پارامتر مشخص شدند.
- [ ] صفحات احراز هویت و بخش‌های حساس بررسی شدند.
URL☐ های تکراری پاک‌سازی شدند.
- [ ] خروجی برای مراحل بعدی آماده شد.

### خروجی مورد انتظار
فهرست کامل URLهای کشف‌شده
فهرست فایل‌های JavaScript
Endpointهای قابل مشاهده
منابع خارجی استفاده‌شده
URLهای دارای پارامتر
مسیرهای مهم برای بررسی‌های بعدی
فایل‌های JavaScript مرتبط با دامنه

## مرحله 6 — شناسایی در کد سمت کاربر
JavaScript Recon
دستور اجرا 1
توضیح دستور
-i :فایل ورودی شامل URLهای فایل‌های JavaScript
:  -o cli نمایش خروجی در ترمینال
دستور اجرا 2
توضیح دستور
-i  :فایل ورودی
-o cli  :نمایش خروجی در ترمینال
فایل ورودی شامل لیست فایل‌های JavaScript استخراج‌شده از مرحله Crawling است.
بهترین شیوهٔ اجرا
فقط فایل‌های JavaScript استخراج‌شده در مرحله Web Crawling بررسی شوند.
نتایج LinkFinder و SecretFinder به‌صورت دستی اعتبارسنجی شود زیرا ممکن است False Positive تولید شود.
Endpointها و اطلاعات مهم در یک فایل مجزا مستندسازی شوند تا در مراحل API Recon و Parameter Discovery مورد استفاده قرار گیرند.
- [ ] فایل‌های JavaScript بررسی شدند.
Endpoint☐ های استخراج‌شده ثبت شدند.
- [ ] مسیرهای API شناسایی شدند.
- [ ] پارامترهای قابل مشاهده ثبت شدند.
- [ ] کلیدها یا Tokenهای احتمالی بررسی شدند.
- [ ] اطلاعات حساس Hardcoded بررسی شد.
- [ ] نتایج مشکوک به‌صورت دستی اعتبارسنجی شدند.
خروجی مورد انتظار:
فهرست فایل‌های JavaScript بررسی‌شده
Endpointهای استخراج‌شده
URLهای داخلی
مسیرهای API
پارامترهای مشاهده‌شده
کلیدها یا Tokenهای احتمالی
اطلاعات حساس احتمالی (Hardcoded Secrets)
موارد نیازمند بررسی دستی
موارد مشکوک به False Positive

## مرحله 7 — کشف محتوا
Content Discovery
دستور اجرا
توضیح دستور
:-uآدرس هدف؛ عبارت FUZZ محل جایگذاری کلمات Wordlist است.
:-wمسیر Wordlist مورد استفاده.
:-mcفقط پاسخ‌هایی با Status Code مشخص نمایش داده شوند.
-o:ذخیره نتایج در فایل.
بهترین شیوهٔ اجرا
ابتدا از Wordlist کوچک و عمومی استفاده شود.
در صورت نیاز، Wordlistهای تخصصی‌تر اجرا شوند.
قبل از تحلیل نتایج، پاسخ‌های پیش‌فرض و False Positiveها حذف شوند.
مسیرهای کشف‌شده بر اساس اهمیت و نوع پاسخ اولویت‌بندی شوند.
- [ ] دایرکتوری‌های جدید شناسایی شدند.
- [ ] فایل‌های قابل دسترس شناسایی شدند.
- [ ] صفحات مدیریتی احتمالی بررسی شدند.
- [ ] فایل‌های Backup یا Config احتمالی بررسی شدند.
Endpoint☐ های جدید ثبت شدند.
Status Code☐ هر مسیر ثبت شد.
- [ ] اندازه پاسخ (Response Size) بررسی شد.
- [ ] موارد False Positive حذف شدند.
خروجی مورد انتظار:
Directoryهای کشف‌شده
فایل‌های کشف‌شده
صفحات مدیریتی احتمالی
فایل‌های Backup یا Config
Endpointهای جدید
Status Code هر مسیر
Response Size
مسیرهای نیازمند بررسی دستی

## مرحله 8 — کشف پارامترها
Parameter Discovery
دستور اجرا 1
توضیح دستور
-d:دامنه هدف برای استخراج پارامترهای آرشیوشده.
دستور اجرا 2
توضیح دستور
-u:آدرس Endpoint مورد بررسی.
-o:ذخیره نتایج در فایل.
بهترین شیوهٔ اجرا
ابتدا با ParamSpider فهرست پارامترهای شناخته‌شده استخراج شود.
سپس Endpointهای مهم با Arjun بررسی شوند.
نتایج هر دو ابزار در یک فایل واحد تجمیع و موارد تکراری حذف شوند.
پارامترهای کشف‌شده بر اساس حساسیت و کاربرد اولویت‌بندی شوند.
- [ ] پارامترهای GET استخراج شدند.
- [ ] پارامترهای POST احتمالی شناسایی شدند.
- [ ] پارامترهای آرشیوشده استخراج شدند.
- [ ] پارامترهای پنهان بررسی شدند.
Endpoint☐ های دارای پارامتر ثبت شدند.
- [ ] پارامترهای تکراری حذف شدند.
- [ ] نتایج به‌صورت دستی اعتبارسنجی شدند.

### خروجی مورد انتظار
فهرست پارامترهای GET
فهرست پارامترهای POST احتمالی
Endpointهای دارای پارامتر
پارامترهای آرشیوشده
پارامترهای پنهان کشف‌شده
پارامترهای نیازمند بررسی امنیتی
موارد نیازمند بررسی دستی

## مرحله 9 — شناسایی رابط برنامه‌نویسی
API Recon
Target
|
▼
API Documentation Discovery
├── swagger.json
├── openapi.json
├── api-docs
├── Swagger UI
└── API Version Information
اگر مستندات پیدا نشد:
|
▼
API Endpoint Discovery
└── Kiterunner
دستور اجرا
توضیح دستور
-A:استفاده از Wordlist مخصوص API Routeها.
-x 10:تعداد درخواست‌های همزمان برای افزایش سرعت اجرا.
:-j 1تعداد Hostهای مورد پردازش.
در تست Narrow معمولاً فقط یک Host بررسی می‌شود.
بهترین شیوهٔ اجرا
ابتدا  Endpointهای استخراج‌شده از مراحل قبلی بررسی شوند:
JavaScript Recon
|
▼
Parameter Discovery
|
▼
API Recon
مستندات API قبل از اجرای Discovery بررسی شوند.
Endpointهای پیدا شده بر اساس اهمیت دسته‌بندی شوند:
Authentication
User Management
File Upload
Admin
Sensitive Functions
پاسخ‌های دارای Status Code مهم به‌صورت دستی بررسی شوند.
- [ ] فایل‌های Swagger/OpenAPI بررسی شدند.
- [ ] نسخه‌های API شناسایی شدند.
Endpoint☐ های API استخراج شدند.
- [ ] متدهای HTTP ثبت شدند.
Endpoint☐ های Authentication مشخص شدند.
Endpoint☐ های حساس مشخص شدند.
API☐ های مستند و غیرمستند ثبت شدند.
- [ ] نتایج Kiterunner اعتبارسنجی دستی شدند.

### خروجی مورد انتظار
فهرست Endpointهای API
متدهای HTTP قابل مشاهده (GET / POST / PUT / DELETE)
نسخه‌های API
فایل‌های مستندات API
مسیرهای مستند و غیرمستند
Endpointهای مرتبط با Authentication
پارامترهای API
موارد نیازمند بررسی امنیتی

## مرحله 10 — نقشه‌برداری احراز هویت
Authentication Mapping
بهترین شیوهٔ اجرا
ابتدا فرآیندهای اصلی با یک کاربر عادی بررسی شوند.
درخواست‌ها و پاسخ‌های مربوط به Login، Logout و Password Reset ذخیره شوند.
تفاوت رفتار سیستم در شرایط مختلف مقایسه شود:
مثال:
Login موفق
Login ناموفق
رمز اشتباه
کاربر بدون دسترسی
Session منقضی‌شده
Tokenها و Cookieهای ایجادشده ثبت و نحوه نگهداری آن‌ها بررسی شوند.
Login Flow
بررسی:
Endpoint☐  ورود شناسایی شد.
Method☐  درخواست (GET/POST) ثبت شد.
- [ ] پارامترهای ورودی ثبت شدند.
Cookie☐  یا Token ایجادشده ثبت شد.
- [ ] رفتار Login موفق و ناموفق مقایسه شد.
Logout Flow
بررسی:
Endpoint☐  خروج مشخص شد.
- [ ] رفتار Session بعد از Logout بررسی شد.
Token☐  یا Cookie بعد از خروج بررسی شد.
Password Reset Flow
بررسی:
- [ ] مسیر بازیابی رمز مشخص شد.
- [ ] روش ارسال Token یا کد تایید ثبت شد.
- [ ] مراحل تغییر رمز مستندسازی شد.
Session Management
بررسی:
- [ ] نوع Session مشخص شد.
- [ ] محل نگهداری Session بررسی شد.
Cookie☐ های مرتبط ثبت شدند.
- [ ] ویژگی‌های Cookie بررسی شدند:
Secure
HttpOnly
SameSite
Session Timeout☐  بررسی شد.
Role Behavior
بررسی:
- [ ] نقش‌های کاربری قابل مشاهده ثبت شدند.
- [ ] تفاوت دسترسی نقش‌ها مشخص شد.
- [ ] صفحات حساس هر نقش ثبت شدند.

### خروجی مورد انتظار
Flow کامل Login
Flow کامل Logout
Flow بازیابی رمز عبور
نوع Session Management
Cookieها و Tokenهای مرتبط
نقش‌های کاربری قابل مشاهده
Endpointهای Authentication
نقاط نیازمند بررسی امنیتی در فاز Vulnerability Assessment

## مرحله 11 — Security Controls Mapping
موارد بررسی
سمت سرور
WAF☐  شناسایی شد.
Rate Limiting☐  بررسی شد.
Account Lockout☐  بررسی شد.
IP Blocking☐  بررسی شد.
Bot Protection☐  بررسی شد.
سمت احراز هویت
MFA☐  بررسی شد.
CAPTCHA☐  بررسی شد.
Session Protection☐  بررسی شد.
سمت مرورگر و Headerها
CSRF Protection☐  بررسی شد.
CSP☐  بررسی شد.
HSTS☐  بررسی شد.
Security Headers☐  بررسی شدند.

### خروجی مورد انتظار
مکانیزم‌های دفاعی شناسایی‌شده
Headerهای امنیتی فعال
محدودیت‌های درخواست
کنترل‌های مرتبط با Authentication

## مرحله 12 — جمع‌بندی یافته‌های Recon
Recon Summary & Documentation

### Final Checklist
پیش از ورود به مرحله ارزیابی آسیب‌پذیری باید موارد زیر آماده باشند:
- [ ] مجوز و Scope تست ثبت شده است.
- [ ] معماری کلی برنامه و مسیرهای اصلی مشخص شده‌اند.
- [ ] صفحات و قابلیت‌های اصلی برنامه مستند شده‌اند.
- [ ] گزارش HTTP Headers و رفتار پاسخ سرور آماده است.
- [ ] وضعیت SSL/TLS و Certificate ثبت شده است.
- [ ] فناوری‌های استفاده‌شده شناسایی شده‌اند.
URL☐ ها و فایل‌های JavaScript استخراج شده‌اند.
Endpoint☐ های استخراج‌شده از JavaScript ثبت شده‌اند.
Directory☐ ها و فایل‌های کشف‌شده ثبت شده‌اند.
- [ ] پارامترهای شناسایی‌شده جمع‌آوری شده‌اند.
Endpoint☐ های API و مستندات احتمالی ثبت شده‌اند.
Flow☐  احراز هویت و Session Management مستند شده است.
- [ ] کنترل‌های امنیتی شناسایی شده‌اند.
GITHUB REPOSITORY STRUCTURE
recommended repository layout:
README.md
docs/
└── Narrow-Recon-Methodology.docx
└── Narrow-Recon-Methodology.md
checklists/
└── recon-checklist.md
evidence/
├── http/
├── tls/
├── crawling/
├── javascript/
├── content-discovery/
├── parameters/
└── api/
templates/
└── findings-template.md
Operational rule: Store only authorized assessment artifacts. Do not commit credentials, session tokens, API secrets, personal data, or other sensitive evidence to a public repository.